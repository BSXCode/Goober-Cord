"use client";

// Legacy file — voice call functionality has been moved to VoiceWidget.tsx
// This export is kept to prevent import errors from any remaining references
export function ActiveCall() {
  return null;
}
